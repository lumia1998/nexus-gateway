# Changelog

本项目遵循语义化版本，记录 Nexus Gateway 面向使用者的重要变化。

## [0.3.1] - 2026-09-10

### 产物管理
- 新增 WebUI「产物」页：跨运行浏览智能体产出的文件，支持图片/视频/音频在线预览、下载与单个删除。
- 新增 `GET /v1/admin/artifacts`（跨运行索引，支持智能体/类型/关键词筛选与分页）与 `DELETE /v1/admin/runs/:id/artifacts/:artifactId`（删除产物文件与记录，运行中的任务拒绝删除）。

### 总览精简
- 统计卡改为一行 4 个并支持点击跳转（智能体 → 智能体页、活动任务 → 运行记录、异常 → 失败筛选的智能体列表）。
- 「智能体状态」面板仅在有异常、未配置智能体或配置读取失败时显示；健康时整块隐藏。
- 系统指标（会话/SSE/内存/配额/存储）合并为紧凑网格，不再独占多张大卡。
- 失败与等待处理列表支持行内快捷取消（等待中任务）与删除记录；超过 5 条显示「查看全部」直达运行记录的失败筛选。
- 运行详情新增「删除记录」（仅限已结束任务）。

### 排版与体验
- 全站字号上调一档（正文 13→14px、辅助 12→13px 等），行高 1.5→1.6，行内标题/副文案间距 2→4px，中文字体密度更舒适。
- 面板内嵌套列表去掉卡片套卡片，改为扁平化分隔线。
- 智能体筛选（协议/状态）纳入 URL hash，可收藏与分享深链接。

## [0.3.0] - 2026-09-10

- 管理控制台支持运行取消、显式输入/授权和新会话重试；重试保留旧记录及原 Key 归属。
- 新增持久化产物、鉴权下载、历史/产物保留策略、按 Key 配额和管理指标。
- WebUI 按页面拆分并加入类型检查；新增通用 stdio ACP、诊断、SSE 恢复和部署冒烟工具。
- 这是破坏性版本：运行列表默认返回 `taskPreview`，配额和历史保留策略可能改变旧客户端行为；详见 README。

## [0.2.5] - 2026-09-02

### 排版统一
- 🔤 全站字号收敛到 `--fs-*` 六级 token（11/12/13/14/16/28px），样式表内不再有散落的像素值
- 📈 统计数字 22px → 28px/600，页面标题 14px → 16px，徽章 10px → 11px
- 🔴 智能体不可用时的错误详情改为 13px + destructive 色，与状态徽章同级可见

### 布局
- 📐 主内容区改为左对齐 + `clamp(24px, 4vw, 48px)` 边距，与顶栏共用同一变量，消除宽屏下视觉重心偏右
- 🧹 修掉上一版遗留的 `--content-max` 悬空引用（变量已删除却仍被 `.page-content` 使用）
- 📊 密钥表行高 66px → 40px 与其余表格统一；运行记录的「开始时间」「耗时」右对齐并启用 `tabular-nums`
- 🗂️ 工作区页改为带数量的面板，每行显示该根目录正被哪些智能体使用

### 交互缺陷修复
- ⌨️ 搜索框每输入一个字符即失焦（重渲染销毁了输入节点），现已保留焦点与光标位置
- 🔄 20 秒就绪轮询不再重渲染与其无关的页面，避免强制关闭已展开的密钥操作菜单
- 👤 侧栏折叠时管理员头像不再被一并隐藏

### 一致性
- 🧩 空态补上主操作按钮，不必自己去右上角找入口；零数据页不再渲染全 0 统计卡与空筛选栏
- 🚫 移除原生 `prompt()` / `confirm()`，重命名与全部破坏性确认改用站内 drawer，确认弹窗默认焦点落在「取消」
- ✂️ 清理复述性界面文案（页面副标题、自动刷新提示、设置页说明卡等）及其失效 CSS

## [0.2.4] - 2026-09-01

### UI/UX 重大升级
- 🎨 WebUI 全面采用 shadcn 设计风格，提升专业度和现代感
- ✨ 新增完整的阴影系统（4 个层级）和微交互动画
- 🎯 优化间距体系：组件间距从 8-12px 提升到 16-32px，视觉更舒适
- 📐 改进排版层级：标题从 24px 提升到 30px，字重和行高全面优化
- 🌈 精细化配色方案：浅色和深色模式都更加柔和协调
- 💫 新增悬停动效：按钮上浮、卡片提升、状态脉冲等流畅过渡效果
- 🔄 动画增强：抽屉滑入、Toast 提示、状态指示器等都有细腻的动画
- 📱 响应式优化：改进移动端断点和小屏幕布局

### 视觉组件优化
- 按钮：高度 40px，悬停上移 1px + 阴影增强
- 卡片/面板：圆角 8-12px，悬停提升效果
- 表格：行高 72px，更好的悬停状态
- 表单：输入框 40px，聚焦时带阴影效果
- 状态徽章：圆角胶囊设计 + 背景色区分

### 部署工具
- 新增多种部署脚本（Bash、PowerShell、Batch）
- 新增详细的手动部署文档

## [0.2.3] - 2026-08-31

- ACP Agent 在文本结果中明确输出 `MEDIA:<path>` 时，Gateway 现在会在该轮完成前将该常规文件快照为受大小限制的二进制 Artifact；不再遗漏 Hermes/ppt-master 写在 skill 目录中的交付文件。
- 已处理的 `MEDIA:` 行不会再出现在最终输出中，避免上游模型根据裸本地路径再次发起“文件在哪里”的追问。

## [0.2.6] - 2026-09-03

### WebUI 设置与即时反馈
- 设置页改为居中的无卡片 Section 布局，运行参数、外观和账户通过留白与 Divider 分隔，输入控件统一宽度。
- 运行参数与控制台密码共用右上角“保存更改”；账户密码字段直接显示在页面内，保存后立即热生效并要求重新登录。
- 侧栏底部拆分为“设置”和“退出登录”两个独立入口；智能体权限策略保存后立即更新列表，无需手动刷新。
- 新增设置布局、侧栏退出和权限策略即时刷新的 Chromium 回归覆盖；本地真实实例完成 139 张截图验收，布局探针与提交流程均无问题。

## [Unreleased]

- 控制台增加取消、以原任务创建新会话重试、补充输入和显式授权；原记录及 Key 归属保留，过期请求不能影响后续轮次。
- 产物支持受鉴权保护的持久化下载，历史与产物分别配置保留期和磁盘预算；外部 URL 只保留元数据。
- 增加按 Key 资源配额、管理指标和显式 Agent 连接诊断；通用 ACP 接入说明在控制台可见。
- 页面按职责拆分，核心前端状态与请求加入类型检查；运行详情独立轮询并保留输入、文本选择和滚动位置，URL 可恢复筛选与详情且不保存搜索文本。
- 前端交互修复：配置迟到时保留密码输入并恢复运行参数编辑；未读取配置时禁止覆盖工作区；清除筛选同步控件与请求；Key 操作采用写入响应并拒绝旧读取覆盖；退出登录清理表单及缓存，阻止迟到操作重新打开密钥抽屉。Chromium 回归增至 42 项。
- 控制台认证后立即显示页面，配置、Key、历史和探测独立加载；失败保留编辑入口并支持单独重试。
- 运行参数与密码独立保存，未编辑字段保留原始毫秒值；首次配置读取失败时不允许用默认参数覆盖配置。
- A2A 区分普通请求/建连 `timeoutMs`、单轮任务 `taskTimeoutMs` 和流失联 `streamIdleTimeoutMs`，长流不再受短请求期限误杀。
- 新增本地配置的通用 `stdio` 驱动及 `probeArgs`，探测复用进程树清理；Windows 支持显式 `.cmd` 和 PATH 中不带扩展名的 npm shim。
- 部署入口统一为固定产物、SHA-256 校验、SSH Key、锁定依赖安装、独立版本目录与健康检查回滚；CI 加入 Windows 和包安装启动检查。
- Key 停用、删除、轮换和 scope 缩小时立即关闭受影响的现有 SSE；已经接受的任务继续，轮换保留 Session 所有权。
- SSE 增加过期、非法及超前游标的 `reset` 控制帧，附带保留游标边界与快照恢复路径；新增带去重、退避和恢复处理的 Node 客户端示例。
- **管理 API 变更：**运行列表用最多 240 字符的 `taskPreview` 替代完整 `task`，默认每页 50 条；详情保留全文，搜索仍覆盖全部已保留任务。
- Agent 部分更新保留已有超时与本地高级字段；清理失败在尝试关闭所有 Session 后上报，CLI 使用失败退出码。
- 本地截图验收后修复窄屏密钥表格重叠、长任务撑宽、授权选项长名称挤压及表格长名称竖排；保留完整操作和横向滚动表格。
- 抽屉按可用视口宽度布局、打开时复位滚动；长表单错误自动滚入视野，通知限高且不再遮挡底部按钮。Chromium 回归累计 15 项。
- 安全：A2A Card 及全部接口强制同源，发现与业务请求拒绝 HTTP 重定向；防止自定义认证头或 Bearer 被发送到 Card 声明的其他源。保留同源 JSON-RPC、HTTP+JSON 和旧版 Card 兼容。
- ACP 初始化及新建会话共享 30 秒握手期限，超时回收进程；连接关闭异常不会跳过后续进程和输入目录清理。会话定时清理逐项处理失败，HTTP Server 的运行期错误触发统一关闭。
- A2A 待输入回复立即接受并释放会话锁，在旧流结束后发送；旧流迟到状态不会重新打开已回答请求。ACP/A2A 取消后都销毁 runtime，后续消息返回 409，不再误报已接受。
- 新增 A2A 认证边界和会话生命周期回归测试；Linux 实测覆盖真实 ACP 子进程及符号链接逃逸拒绝。
- 控制台搜索框和筛选器在轮询期间保留节点，修复中文输入法组合态被打断；整页播报改为去重后的状态摘要。
- 导航按运行与网关配置分组，设置页集中管理运行参数、主题及账户；补齐状态色对比度、减少动态效果偏好及键盘可访问性。
- 运行统计基于全部保留记录，列表明确说明最近 200 条的筛选范围；删除仍被 ACP 配置引用的工作区时提供可操作的冲突提示。
- 表格详情使用真实按钮；抽屉支持焦点循环、焦点归还及背景锁定；密钥菜单支持方向键、Home/End 和 Escape。
- 启动和运行详情增加加载反馈，提交按钮防重复提交，迟到响应不会覆盖新抽屉；相同结果跳过 DOM 替换并复用日期格式器。
- 新增 11 项 Chromium WebUI 回归测试，CI 增加浏览器测试及生成物漂移检查。
- 安全：`MEDIA:<path>` 声明的文件现在必须解析到配置的 `workspaceRoots` 之内才会被快照为 Artifact，越界路径（含 `file://` 形式）会被拒绝，并在会话事件中说明原因。此前这条路径不做任何边界检查，Agent 输出的任意绝对路径都会被读取并以 base64 返回给调用方。
- 单轮最多附加 8 个 `MEDIA:` 产物，超出部分会在事件中报告；单个文件上限由 32 MiB 收紧到 12 MiB，与工作区发布接口一致（12–32 MiB 的文件此前会被完整读取、编码后再静默丢弃）。
- 运维提示：若 Agent 把交付文件写在工作区之外（例如 skill 目录），需要把该目录显式加入 `workspaceRoots`，否则产物不再被附加。
- 恢复 ACP `allow`（始终允许）权限策略：WebUI、配置校验与运行时一致支持，无需逐次确认。
- WebUI 重构为 Agent Nexus 管理控制台，并拆分为独立的样式、结构、图标和行为模块。
- 管理员 Console Password 改为 scrypt 哈希与 HttpOnly Cookie；客户端改用可命名、可限制 Agent
  范围的多 API Key。
- 兼容迁移旧 `authToken` 为数据面 Legacy Access Key，旧 Agent 与 Workspace 配置保持不变。
- 新增基于官方 SDK 的 A2A Agent Card、消息流、任务状态、Artifact 和取消支持。
- 新增认证失败限速、请求/提示超时、Session/SSE/连接容量和隐藏内部 500 错误。
- 明确支持 `0.0.0.0` 局域网监听并打印可用 LAN 地址。
- WebUI 全面改为简体中文，包括初始化、登录、导航、表单、状态、提示和日期格式。
- API 密钥列表改为紧凑信息层级，常用操作使用图标，低频与危险操作收纳到更多菜单。
- 修复智能体状态并发加载时错误显示“尚未检查”，统一以总览探测结果为状态源。
- A2A 配置改用完整 Agent Card URL，支持自动、JSON-RPC、HTTP+JSON 首选传输，并兼容旧 `agentUrl` 根地址配置。
- 新增持久化运行记录：每个任务记录原始请求、真实阶段、状态、结果摘要、耗时和产物，重启后
  保留历史并将未完成任务标记为中断。
- 新增仅管理员 Cookie 可访问的 `/v1/admin/runs` 列表/筛选与详情 API，以及全中文“运行记录”
  页面，区分当前运行和历史记录并每 5 秒自动刷新。
- 修复 Session message 入站会 `trim()` 用户任务的问题，现仅判空并保持原文交给 Agent。

## [0.1.4] - 2026-08-25

- 将 Gateway 拆分为独立仓库。
- 提供首次安装引导，由管理员手动设置至少 8 位的 Access Key。
- 新增独立 WebUI，支持 Agents、工作区白名单、密钥管理和亮色/暗色主题。
- 支持 OpenCode、Claude Code、Codex、Pi、OpenClaw 与 Hermes ACP Driver。
- 提供 Bearer 认证的 Agent inventory、Session、SSE 事件和控制面 API。
- 配置更新采用权限受限的临时文件原子替换，并热重载 Agent registry。

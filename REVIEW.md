# Nexus Gateway 项目评审与开发路线

2026-09-08 更新 · 当前版本 0.2.6

## 扩展功能与 P2 集成验收（2026-09-09）

2026-09-09 补充：存储故障回归验证历史文件原子替换失败后保留旧数据，解除故障后 `flush()` 可恢复；损坏、超大产物在下载时拒绝。指标读取复用最近成功持久化的历史大小，避免轮询全量序列化。重试去重状态限制为 512 条成功记录/24 小时，失效目标可回收。

本机复跑 `npx tsx scripts/artifact-storage-benchmark.mjs 100 4096`：100 个 4 KiB 产物入队 5.49 ms、flush 257.84 ms，总计 263.39 ms，落盘 409600 字节、排队内容归零。容量脚本一次受控测试中，8 次创建有 4 次成功/4 次配额拒绝，4 个 SSE 有 2 个成功/2 个拒绝；这些是功能及本机容量基线，不能推导生产吞吐量。

按已确认的扩展范围，控制台现已接入取消、输入回复、显式授权，以及保留旧记录的新会话重试。重试保留原 Key 归属；截断任务和输入附件历史拒绝自动重放。操作会在会话锁内校验当前 Run 与请求，防止旧记录操作新一轮任务。

产物增加持久化快照与鉴权下载，外部 URL 不主动抓取。历史和产物分别设置保留期、容量预算，写入失败保留重试状态。按 Key 配额、管理指标及显式连接诊断已接入。新重试在去重检查后才预留会话和运行额度，失败回滚；终态释放运行额度，关闭会话释放会话额度。满额时重复重试仍返回已有任务。

前端由 GPT-6 完成：按页面拆分渲染，核心状态和 API 加入 TypeScript 检查；详情独立轮询、保留输入和输出选择/滚动，历史新增提示不移动当前页，URL 恢复筛选和详情但不保存搜索文本。Session 的管理操作、准入与探测以及控制面校验分离为模块，保留原接口，未迁移框架或存储引擎。

回归覆盖：类型检查、后端 109 项（105 通过、4 项平台跳过）、Chromium 53 项，包含真实网关和详情 URL 刷新恢复。真实 RunStore/SessionManager/HTTP 联测确认重启后原 Key 可下载持久化产物，其他归属、错误 scope、过期及仅元数据产物被拒绝。Windows 合成列表基准约为 50 条 1.4 ms / 51 KB、200 条 3.7 ms / 205 KB，仅表示本机测试数据的渲染结果，不代表生产并发容量。最终构建和安装包验收结果见下方记录。

后文早期“剩余路线”及测试数量保留为历史评审记录，以本节和后续最终验收记录为准。真实 Linux/systemd/SSH 部署和 Windows 父进程退出后的孤儿进程回收边界仍未在本轮得到新增实机证明。

最终本机验收（2026-09-09）：`npm run typecheck`、`npm test`（109 项，105 通过、4 跳过）、`npm run test:webui`（53 通过）、`npm run build` 和 `git diff --check` 均通过。已重新生成 `nexus-gateway.tar.gz`，`node scripts/package-smoke.mjs nexus-gateway.tar.gz` 的解包、锁定依赖安装、包安装、CLI 启动与 HTTP/UI 冒烟通过。工作区修改保留，未提交或部署。

## GPT-6 前端专项修复（2026-09-08）

根据前端工作使用 GPT-6 的要求，本轮由 GPT-6 主代理直接复核和修改前端。在上一轮 Luna 实现基础上补齐以下交互边界：

- 配置迟到或重试成功时，就地恢复运行参数输入与保存；保留密码输入节点、内容和焦点。首次配置读取失败时禁止编辑智能体和工作区，避免以空根目录列表覆盖配置。
- 运行记录的“清除筛选”按钮随筛选条件显示，清除后同步搜索框和下拉框，取消待发送的搜索请求，避免重复绑定点击事件。
- Key 重命名、启停、scope 更新和轮换直接采用成功响应刷新界面；轮换后的新密钥不会因附加列表读取失败而丢失。保存前发起的旧列表读取不能覆盖已确认的新值。
- 退出登录清理旧配置、历史和密码表单；跨登录周期迟到的读写响应不能恢复旧数据或重新打开密钥抽屉。
- 修正小于一小时/一分钟的范围错误提示被取整为零，以及 `hidden` 被按钮布局样式覆盖的问题。

验证：新增 7 项 Chromium 回归，`npm run test:webui` 共 **42 项全部通过**；`npm run typecheck`、`npm run build` 和 `test/server.test.ts` 的 **5 项服务回归**通过。后文的 35 项浏览器测试数属于上一轮记录。

## 本轮实施记录（2026-09-07）

本轮以 P1 核心链路为范围，并完成与其相邻的历史列表、SSE 恢复和通用 ACP 工作。前端、运行时和部署由 Luna 子代理实施，主代理完成集成审查、边界修复与最终验证。以下记录表示当前工作区状态；后文保留本轮开始时的评审依据，其中“当前问题”和旧测试数量不是修复后的状态。

| 工作项 | 本轮结果 | 验证及边界 |
| --- | --- | --- |
| P1 首屏独立加载 | 认证成功即进入控制台；配置、Key、历史和探测独立加载及重试 | 慢探测不阻止设置编辑；首次配置失败禁止提交默认参数；迟到结果保留未保存表单 |
| P1 设置独立保存、精度统一 | 密码与运行参数使用独立表单；未编辑值保留毫秒精度；连续保存可恢复原值 | 浏览器覆盖两种独立保存、最低有效值、小数、连续保存和 stdio 编辑；高级命令、参数及环境字段仍由本地配置管理 |
| P1 A2A 超时拆分 | `timeoutMs` 限制普通请求及建连，`taskTimeoutMs` 限制单轮任务，`streamIdleTimeoutMs` 限制流空闲 | 实际 HTTP 测试覆盖慢 Card、持续长流、停滞、任务期限和主动取消；省略新字段时分别继承全局任务期限与请求超时，显式清空可恢复继承 |
| P1 Key 撤销闭环 | 停用、删除、轮换和缩小 scope 后关闭受影响的现有 SSE | 执行中的任务继续；其他 Key 不受影响；轮换保持 Key ID，新密钥可继续访问原 Session；客户端遇到鉴权失败停止重连 |
| P1 部署流程收口 | 统一 `nexus-gateway.tar.gz`、SHA-256、SSH Key、`npm ci --omit=dev`、版本目录切换、systemd 与健康失败回滚 | 已提供安装包冒烟和本地模拟部署回归；尚未执行真实 SSH/systemd 部署 |
| P1 跨平台进程验收 | Windows 命令入口、带空格的 PATH/cwd、npm shim、启动截止时间、取消和探测树清理已覆盖；关闭失败返回错误 | Windows 本机通过；已加入 Ubuntu/Windows CI，Linux 本轮结果待运行。Windows 父进程已退出后的孤儿进程仍需 Job Object 等机制，不能宣称完全回收 |
| P2 历史列表轻量化 | 默认每页 50 条，`taskPreview` 最多 240 字符；错误与进度摘要也设上限，详情及搜索保留全文 | 大文本测试约束 50 条响应小于 110 KB；尚未进行浏览器性能基准。管理列表 API 的完整 `task` 已由 `taskPreview` 替代 |
| P2 SSE 恢复与客户端 | `reset` 帧说明过期、非法及超前游标，提供游标边界和快照路径；增加 `examples/client.mjs` | 覆盖有界控制帧、快照游标恢复、去重、退避、输入、授权和取消；快照只恢复当前状态，不能补回已淘汰事件 |
| P2 通用 ACP 与探测清理 | 增加明确的 `stdio` 驱动和本地 `probeArgs`；探测等待有截止时间的进程树清理 | 自定义配置和已有预设兼容；成功退出的 POSIX 探测清理测试交由 Linux CI |

本轮本机验收环境为 Windows / Node 24.19.0。`npm run typecheck` 与 `npm run build` 通过；`npm test` 共 86 项，82 通过、0 失败、4 跳过；`npm run test:webui` 的 Chromium 测试 35 项全部通过。4 项跳过包含 3 项现有 Windows 符号链接用例和 1 项 POSIX 父进程退出后的探测清理用例。CI 使用 Node 20，须以对应提交实际运行结果作为发布依据。

最终产物也已重新打包并通过 `node scripts/package-smoke.mjs nexus-gateway.tar.gz`：解包、锁定依赖安装、生成及安装 npm 包、CLI 启动、初始化、登录、创建 Key、数据面认证、健康检查和内嵌 UI 均成功。Windows 打包显式使用系统 `tar.exe`，避免 Git Bash 的 GNU tar 将盘符误当远程主机。Bash/PowerShell 语法检查通过；Git Bash 原生符号链接模式下的本地部署模拟验证了成功切换、依赖安装失败、校验失败和健康失败回滚，不能替代真实 Linux/systemd 验收。

剩余路线：按 Key 配额、历史保留期与磁盘预算、持久化故障及性能基准、指标、总览/详情体验完善、前端职责拆分与类型检查；P3 的长期产物下载、管理员执行操作和数据库/多用户仍按需求推进。上述项目未被标记为本轮完成。

## 1. 总体判断

Nexus Gateway 已具备实用轻量网关的基本结构：以 HTTP/SSE 对接客户端，以 ACP 管理本地 Agent 进程，以 A2A 接入远端 Agent，控制台负责配置和观察，运行历史保留任务结果。认证、会话归属、权限请求、完成状态、文件边界和资源限制都有实际实现，已经超过简单协议转发器的阶段。

当前主要短板集中在三个方面：

1. **核心使用链路不够一致。** 慢探测影响进入控制台；设置会取整改写参数；A2A 超时含义与界面不同；撤销 Key 的实际效果与提示不一致。
2. **容量与恢复设计需要补齐。** 大任务列表响应偏重，历史全量重写，SSE 事件过期缺少明确恢复信号，资源配额主要是全局限制。
3. **交付方式落后于核心代码。** 部署脚本不统一，跨平台关键测试有缺口，前端类型保障不足，使用文档需要与实际部署路径统一。

下一阶段以“稳定接入、准确状态、可恢复任务、可重复部署”为主线。继续保持单进程、轻依赖、内嵌控制台的形态，先提高使用成功率和维护效率。

## 2. 产品定位与方向把控

核心用户是在自己管理的机器或服务器上运行 Agent 的开发者和管理员；调用方包括机器人、桌面程序、自动化脚本和其他服务。

项目的核心价值：不同 Agent 共享会话、消息、事件、权限、取消和结果接口；客户端准确知道任务执行到了哪里；管理员能定位配置和运行错误。

### 应坚持的设计

- 保留 `0.0.0.0` 默认监听与 LAN 接入，用初始化凭证、鉴权和管理入口配置保护服务。
- 保留 ACP 与 A2A 两条路径，驱动预设承担快速接入职责。
- 保留 Key 限定 Agent 范围、Session 绑定创建者的授权模型。
- 工作区决定 cwd 和网关文件访问范围；执行不互信任务时另设系统账号或容器隔离。
- 控制台以配置和观察为核心，输出按安全文本展示。

### 扩展顺序

| 扩展方向 | 判断 | 前置条件 |
| --- | --- | --- |
| 通用 stdio ACP 驱动 | 值得做 | 定义命令、参数、环境变量、探测与校验，兼容已有预设 |
| 控制台取消、重试、输入与授权 | 后续讨论 | 先定义管理员跨 Key 操作权、审计和重试语义 |
| 历史产物下载 | 独立功能 | 持久存储、所有权、保留期和下载鉴权 |
| SQLite 等存储 | 测量后决定 | JSON 写入延迟、内存或数据规模达到明确瓶颈 |
| 多用户、多租户 | 独立产品阶段 | 有真实隔离需求及进程、文件、网络、额度隔离能力 |
| 前端框架迁移 | 当前优先级低 | 先完成模块、状态与类型整理，再评估复杂度 |

## 3. 架构与可维护性

`index.ts` 组装配置、控制面、会话、历史与 HTTP 服务；`server.ts` 负责入口；`session.ts` 管理会话；runtime 适配协议；WebUI 独立内嵌。结构适合当前规模，主要问题是模块内部职责持续累积。

| 模块 | 当前问题 | 整理方向 |
| --- | --- | --- |
| `src/server.ts` | 路由、鉴权、限速、来源校验、上传和 SSE 集中 | 分出管理路由、数据路由、认证策略和连接管理 |
| `src/session.ts` | 状态、附件、历史、容量、探测、清理集中 | 先提取 readiness 与容量管理，明确 Session/Run 边界 |
| `src/control-plane.ts` | 配置持久化与多类管理操作集中 | 保留统一原子写入口，分离校验和领域操作 |
| `src/webui/app/render.js` | 渲染、表单提交、详情和 Key 操作混合 | 按页面拆分，提取数据加载和写入状态 |

重构跟随功能修复，每次围绕一项职责推进。重点是减少重复状态和隐式副作用，避免仅按文件长度拆分。

## 4. 会话、任务与协议运行

### 4.1 Session 与 Run

项目已区分会话与任务历史，完成需要协议证明及实际结果；ACP 的 token/turn 限制不会直接成为成功，等待输入时不会误标完成。这是应该保留的关键设计。

需要统一以下契约：

- 一个 Session 可以承载多轮 Run，完成一轮与释放 Session 不同。
- 取消、任务超时、客户端断线、Key 撤销和网关重启分别如何影响会话与任务。
- 协议结束但没有文本或产物时，客户端收到什么错误。
- 失败原因区分协议异常、进程退出、超时、重启中断，避免只给笼统的 failed。
- 完成证明验证协议结束及结果存在，业务结果质量由调用方判断。

建议维护状态转换表，让 HTTP、SSE、历史与前端标签共用定义。验收必须覆盖等待状态、取消后迟到结果、多轮任务和异常退出。

### 4.2 ACP 接入

六种驱动预设共用 stdio 底层，支持 command、args、环境变量与工作区，接入结构合理。

待完善：

1. **增加通用驱动。** 当前注册表按六种 driver 分支，自定义命令仍借用某个预设。新增明确的 stdio 驱动及可配置探测方式，避免名称与探测参数继承其他产品。
2. **统一探测清理。** `drivers/stdio.ts` 的探测超时只调用 `child.kill()`；任务运行时已有进程树清理。自定义命令创建子进程或忽略退出信号时，探测可能留下进程。应复用有截止时间的清理并确认退出。
3. **区分命令可用与握手可用。** 自动轮询保持低成本；握手诊断由管理员显式触发，分阶段展示启动、初始化和会话建立结果。
4. **补齐 Windows 启动验收。** 覆盖 npm 命令入口、带空格路径、必要环境变量、取消和子进程树退出。按真实 Agent 启动需求检查环境变量继承列表。

### 4.3 A2A 接入

Card 发现、认证、传输选择、流式消息、任务状态和取消已接入；接口同源检查与禁止重定向能限制认证值的发送范围。保留私网 A2A 符合产品用途。

**当前优先问题：请求超时和任务期限耦合。** `src/a2a/runtime.ts` 用 `Math.min(promptTimeoutMs, config.timeoutMs)` 设置任务计时器，但前端把 timeoutMs 解释为单次请求上限。正常长任务可能因较短请求超时而提前结束。

需要分清 Card/普通请求超时、任务总期限、流失去进度后的等待策略。新增字段同时明确旧配置映射，覆盖长任务、服务失联、流提前结束、等待输入和主动取消。

## 5. 控制面、鉴权与配置

初始化 token、管理员密码与 API Key 分离、管理来源校验、scope、Session 创建者绑定、实时 Key 鉴权、失败限速和 reveal 审计均已实现，基础较完整。

### 5.1 Key 撤销要贯穿现有连接

`server.ts` 只在建立 SSE 前检查 Key 与 scope，建立后不主动响应撤销；`webui/app/main.js` 的删除和轮换提示却使用“立即失去访问权限”等措辞。

按 Key/Agent 跟踪 SSE，在停用、删除、轮换和 scope 缩小时关闭受影响连接。同步规定执行中任务继续还是取消，以及删除 Key 后任务如何收尾；轮换后新密钥能否继续原 Session 也要写清楚。

界面分别说明对新请求、现有连接和正在执行任务的影响，不能把断开输出描述成任务停止。

### 5.2 设置必须无损保存

`webui/app/render.js` 把毫秒取整成小时、分钟、秒，保存时乘回原单位，且修改密码也会先写运行参数。

- TTL=90 分钟显示为 2 小时，保存后原值改变。
- 仅修改密码也会触发运行参数改写。
- 后端允许 TTL=1 分钟、任务超时=10 秒，UI 取整为 0 后可能被最小值校验拦截。

密码与运行参数应独立保存，单位转换无损，前后端范围一致。未编辑字段不能因打开页面并保存而改变。

### 5.3 明确热更新影响

将字段分为立即生效、仅影响新会话、重启生效三类。Agent 参数、工作区和 scope 变化对已有会话分别定义，在界面同步标注。

本地 command、args、inheritEnv、env 保持高级配置入口，WebUI 编辑其他字段时继续保留它们。配置持久化失败必须明确反馈，避免重启后才发现改动未保存。

## 6. SSE、容量与资源治理

### 6.1 断线恢复需要历史缺口信号

`sse-writer.ts` 已限制缓冲和背压时间，重放与心跳走同一边界。剩余问题在客户端恢复协议。

`events.ts` 按数量淘汰旧事件，`after()` 仅按序号过滤。客户端游标早于最早保留事件时没有明确 gap/reset 通知。

增加缺口信号、最早/最新游标和快照恢复规则，处理非法及超前游标。客户端示例包含去重、退避和缺口处理；快照恢复当前状态，已淘汰事件不能当成完整历史补回。

### 6.2 增加按 Key 配额

已有 Session、SSE、连接、附件、输出、探测并发限制，Session 创建也在异步步骤前预占容量。下一步应限制单 Key 的会话、执行并发、SSE 和上传预算，避免一个合法客户端占满全局资源。

429 应返回可识别的额度类型与重试提示；容量紧张时仍需保留管理诊断能力。

### 6.3 用测量确定默认值

记录 RSS、会话数、探测排队、活跃 SSE、事件字节、启动延迟、列表响应大小、历史写入延迟和磁盘增长。测试多客户端、慢消费者、大输出、大附件、慢磁盘与频繁状态变化，再决定配额和存储升级。

## 7. 历史、附件与产物

### 7.1 列表响应需要轻量化

`RunStore.publicView()` 去掉 output/artifacts，但保留完整 task；前端每页请求 200 条，每 5 秒刷新，列表及详情按钮 aria-label 均使用完整任务文本。

任务接近上限时，分页仍会带来很大响应、克隆及 DOM 开销。列表应返回固定长度 taskPreview、结果摘要、状态、时间和 ID；详情按需返回全文。保留服务端全文搜索，默认页大小调整为 25/50，并测量响应字节与渲染时间。

### 7.2 历史存储的演进

当前默认保留 1000 条，具备 250ms 合并写入、原子文件替换、损坏文件备份和重启中断标记，适合小规模服务。

改进方向：

- 降低全量克隆、序列化与文件替换成本。
- 慢磁盘下合并排队快照，避免保存大量过时版本。
- 明确写入失败重试、临时文件清理、磁盘满、fsync 和断电恢复行为。
- 将硬编码条数扩展成可配置条数、保留天数及磁盘预算。
- 明确运行中记录保留规则，搜索范围标注为“已保留历史”。

先建立持久化接口与基准，达到瓶颈后再考虑 SQLite。

### 7.3 文件交付契约

附件、显式发布与 MEDIA 各有路径及容量边界，需要统一整理权限范围、大小限制、保留时间和获取方式。避免客户端混淆 Session 工作区与全局来源根。

历史产物只有元数据时，页面显示名称、类型和数量。长期下载功能需要独立持久存储、过期清理、所有权和下载授权，不能仅给历史名称加链接。

## 8. 前端评审与修改方案

现有黑白主题、侧栏、表格和抽屉适合管理控制台，已有窄屏、键盘菜单、焦点、组合输入和轮询竞争测试。下一轮重点是信息准确、错误可定位、阅读与操作稳定。

### 8.1 最高优先级：首屏加载与探测解耦

`screens.js` 的 enterApp 等待 loadAll；`data.js` 用 Promise.all 等待配置、Key、overview 和历史；overview 又等待全部 Agent 探测。前端读取期限 15 秒，A2A 请求可能等待 60 秒。

一个慢 Agent 即可使登录后的初始化超时并重新显示登录页，管理员无法进入配置页修复它。

认证成功即显示应用，各资源独立加载；配置优先可编辑，探测显示“检查中”；普通失败留在对应区域，只有认证失效才转登录。错误区提供上次成功时间及重试入口。

### 8.2 总览用于定位问题

当前总览偏重数量统计；全部 Agent 停用时，checking 和 attention 都为零，仍可能出现“全部就绪”。

建议：

1. 顶部展示连接状态、刷新时间、启用 Agent 数、异常数与活动任务数。
2. 中部列 Agent 错误，提供编辑和重新检查入口。
3. 下部列最近失败及等待输入/授权任务，可进入详情。

区分未配置、全部停用、检查中、命令/Card 可用、探测失败；会话数与活动任务数使用不同名称。

### 8.3 运行页与详情

- 列表使用短任务预览，保留结果摘要、状态、耗时和时间。
- 提供清除筛选、页大小、当前筛选结果说明。
- 用非敏感 URL 状态恢复页面、筛选和运行 ID，不放任务全文或凭据。
- 历史翻页时用“有新任务”提示减少列表跳动，之后再考虑游标分页。
- 详情按状态、进度、结果、错误、产物分区更新，保留滚动和选区。
- 增加“跟随最新”、复制结果、复制错误和复制运行 ID。
- 详情读取独立调度，避免列表变慢后连带延迟详情。
- 取消、失败、等待的统计口径统一；授权仍由创建任务的客户端处理。

### 8.4 表单与操作反馈

Agent 保存/删除后会等待 loadAll。写入成功、读取失败可能进入同一提交异常路径，用户容易误判并重复操作。

统一为保存中 → 保存成功 → 更新展示；后续读取失败明确提示“已保存，刷新失败”。密码与运行参数独立保存，字段旁提供校验错误，显示未保存修改。

智能体页展示协议、启用状态、最近探测、最近会话结果和实际命令摘要；自动单次授权说明缺少单次选项会取消，不会自动转询问。

Key 页面说明 scope 与会话所有权区别；禁用、删除、轮换提示准确反映现有流和任务行为。

### 8.5 工程质量

按页面拆分 render.js，为 api/data/state 增加 JSDoc 或逐步迁移类型检查。当前 checkJs=false，构建通过没有覆盖完整前端类型。

维持键盘操作、焦点恢复、状态播报、窄屏按钮、长路径、长错误及浅深主题回归，补充首屏降级、精度保存和大文本性能测试。

## 9. 部署与运维

### 9.1 部署工具需要统一

当前问题：

- `deploy-remote.sh` 生成 nexus-gateway-deploy.tar.gz，Windows 脚本使用 nexus-gateway.tar.gz。
- Bash 仅显式检查构建结果，上传、打包、SSH 失败后可能继续并显示完成。
- PowerShell 未统一检查外部进程退出码，credential 对象未用于实际连接。
- Windows 脚本引导将密码写进源码，并通过 PuTTY 命令行传递。
- 部分完成提示固定为 10.1.2.40，与目标配置无关。
- nohup 启动缺少统一健康检查、服务管理、升级切换与回滚。

统一流程：固定版本构建产物 → 校验 → SSH Key 上传 → 锁定依赖安装 → 独立版本目录 → 重启服务 → 健康检查 → 失败回滚。平台脚本仅作为这套流程的入口。

使用 `npm ci --omit=dev`；提供 systemd 示例，写清运行账号、工作目录、配置路径、日志、Setup token 获取和代理配置。任何一步失败都应终止并返回失败。

### 9.2 运行数据独立管理

`.gitignore` 只忽略默认配置和默认历史名，自定义 local 配置与历史仍出现在未跟踪列表。为运行数据指定独立目录并忽略，保留可提交的 example 配置。

配置、历史、日志、临时附件各有固定位置、保留策略和备份方式，方便迁移、升级及回滚。

### 9.3 可观测性

已有 requestId 和部分结构化错误日志；进一步关联 Key ID、Agent ID、Session ID、Run ID 与失败阶段，统计启动失败、探测失败、任务超时、SSE 断开和历史写入失败。

进程存活、控制台可用与 Agent 探测分开；健康端点不依赖全部远端 Agent 可达。关闭失败应通过日志与退出状态准确反映。

## 10. 测试与发布质量

当前本机类型检查及构建通过；后端 69 项测试中 62 通过、7 项 Windows 平台跳过；浏览器 27 项通过。CI 配置为 Ubuntu / Node 20，下一版本应以对应提交的实际 CI 结果验收。

现有覆盖包含认证、scope、会话归属、并发、探测、背压、权限、历史、A2A 和主要前端交互。下一轮重点补边界：

| 场景 | 验收目标 |
| --- | --- |
| 慢 Agent 首屏 | 超过 15 秒仍可进入控制台编辑配置 |
| 设置精度 | 原值无损；单独改密码不提交运行参数 |
| A2A 长任务 | 请求超时不误杀长任务，失联及时失败 |
| Key 撤销 | 新请求、旧流、其他 Key、scope 与任务处置一致 |
| SSE 事件过期 | 能检测缺口并恢复当前状态 |
| 进程生命周期 | Linux/Windows 分别验证启动、取消、探测超时及子进程退出 |
| 存储故障 | 慢写、失败、磁盘满、损坏与重启恢复 |
| 大历史列表 | 响应和渲染成本由预览长度及页大小约束 |
| 安装产物 | 从包安装后启动，内嵌 UI 与所有模块完整 |
| 部署失败 | 各步骤正确失败，旧版本可恢复 |

发布绑定具体提交与产物：跨平台检查 → 打包安装 → 启动初始化 → 合成任务闭环 → 版本与变更记录。未提交的新模块和测试必须进入同一发布提交。

## 11. 开发优先级与里程碑

P1 是下一轮核心链路工作；P2 为恢复、性能和体验；P3 由实际需求驱动。

| 优先级 | 工作项 | 完成标准 |
| --- | --- | --- |
| P1 | 首屏独立加载 | 慢 Agent 不阻止进入配置页 |
| P1 | 设置独立保存、精度统一 | 未编辑值不变，改密码不写运行参数 |
| P1 | A2A 超时拆分 | 请求、任务、流失联含义与测试明确 |
| P1 | Key 撤销闭环 | 文案与行为一致，旧流和任务有处置规则 |
| P1 | 部署流程收口 | 统一产物、退出码、服务管理与回滚 |
| P1 | 跨平台进程验收 | 支持平台上有真实启动、取消、清理结果 |
| P2 | 历史列表轻量化 | 列表/详情分离，性能有基线 |
| P2 | SSE 恢复及客户端样例 | 可演示断线、缺口、输入、授权和取消 |
| P2 | 总览、详情、表单反馈 | 状态可信、错误可定位、长输出可读 |
| P2 | 通用 ACP 与探测清理 | 自定义接入不借用预设，探测子进程可回收 |
| P2 | 配额、存储和指标 | 有预算、故障恢复与测量结果 |
| P2 | 前端模块与类型 | 页面职责清楚，核心状态和 API 数据受检查 |
| P3 | 产物持久化 | 长期下载需求明确后实施 |
| P3 | 控制台执行操作 | 管理员权限与审计确定后实施 |
| P3 | 数据库、多用户 | 容量数据与实际需求触发 |

建议按四个里程碑推进：

1. **稳定版收口：**完成 P1，形成可重复部署和升级路径。
2. **通用接入闭环：**通用 ACP、最小 HTTP/SSE 客户端、错误码与状态契约。
3. **控制台与容量优化：**历史轻量化、异常定位、详情阅读、Key 配额、存储基准。
4. **需求驱动扩展：**据实际使用决定产物持久化、管理员操作及存储升级。

每轮用具体结果判断是否完成：客户端能接入、任务状态准确、失败能恢复、管理员能定位、部署能回滚。后续投入围绕这些结果安排。
